export default function App() {
  return (
    <div className="min-h-screen bg-zinc-950 text-zinc-100 font-mono">
      <header className="flex items-center gap-4 border-b border-zinc-800 px-6 py-4">
        <h1 className="text-sm font-bold tracking-[0.3em] text-zinc-50">
          CLUSTER MEMPOOL OBSERVER
        </h1>
      </header>

      <main className="p-6">
        <p className="text-xs text-zinc-500">
          Scaffold shell — POC views live in <code>web/poc/</code>, ported to
          React later.
        </p>
      </main>
    </div>
  );
}
