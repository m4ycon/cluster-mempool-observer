[package]
name = "testkit"
version.workspace = true
edition.workspace = true

[dependencies]
observer = { path = "../observer" }
shared = { path = "../shared" }
api = { path = "../api" }
corepc-client.workspace = true
# Spawns a regtest bitcoind for integration tests
corepc-node = { version = "0.12.0", default-features = false, features = [
  "30_2", # TODO: update to 31_0 once available, it will allow us to test cluster mempool related features
  "download",
] }
# Postgres for diesel-async integration tests.
diesel-async.workspace = true
metrics.workspace = true
metrics-exporter-prometheus.workspace = true
serde_json.workspace = true
testcontainers.workspace = true
testcontainers-modules.workspace = true
time.workspace = true
tokio.workspace = true

[features]
# Treat warnings as a build error.
strict = []
