apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus # uid is fixed: the provisioned dashboards reference it.
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s
